# Direct runtime dependencies only, pinned. Transitive dependencies are
# resolved by pip — do not paste `pip freeze` output in here, or a real
# dependency becomes indistinguishable from something it dragged along.

alembic==1.16.5
email-validator==2.3.0
fastapi==0.128.8
httpx==0.28.1
psycopg2-binary==2.9.11
pydantic==2.13.0
pydantic-settings==2.11.0
SQLAlchemy==2.0.49
uvicorn==0.39.0
